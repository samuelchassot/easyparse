package build

import mill.*, scalalib.*

object easyparse extends ScalaModule {
	def scalaVersion = "3.8.3"
	def publishVersion = "0.3.0"
	def millSourcePath = os.pwd / "easyparse"
	def mvnDeps = Seq(
		mvn"com.lihaoyi::sourcecode:0.4.4"
	)
}
